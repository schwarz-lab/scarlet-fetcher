{
  "name": "scarlet-fetcher",
  "version": "1.0.0",
  "description": "Some ping service (to keep the site up? not tested yet, can't think of a name for a project.) ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    "start": "npm install && node ."
  },
  "author": "Nexus Labs Ltd.",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "node-fetch": "^2.6.1"
  }
}
